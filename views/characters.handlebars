{{!-- If a user is logged in, show this button --}}
{{#if loggedIn}}
<button class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#add-character" style="display: block; margin: 0 auto;">
    Create a new Character!
</button>
{{/if}}

{{!-- For every character in the database, create these elements --}}
{{#each characterDataPlain as |characters|}}
<a href="/characters/{{characters.id}}" style="text-decoration: none;">
    <div class="card character-card">             
        <div style="display: flex; flex-direction: row; justify-content: space-around">
            <h3 style="color: red;">{{characters.name}}</h3>
            <a href="/users/{{characters.author}}" style="text-decoration: none; color: red;">
                <h3>Created by: {{characters.author}}</h3>
            </a>
        </div>
        <p>{{characters.description}}</p>
    </div>
</a>
{{/each}}

{{#if loggedIn}}
{{> add-character-modal}}
{{/if}}